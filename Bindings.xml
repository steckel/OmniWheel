<Bindings>
  <Binding name="Toggle OmniWheel" header="Omni Wheel" runOnUp="true">
    if keystate == "down" then
      omni_wheel_addon:ShowWheel();
    else
      omni_wheel_addon:HideWheel();
    end
  </Binding>
</Bindings>
